import { Container } from "react-bootstrap";

export default function Home() {
    return (
        <Container>
            <p>We build accessible, responsive websites using the latest standards.</p>
        </Container>
    );
}